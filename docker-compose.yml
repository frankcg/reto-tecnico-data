version: '3.8'

services:
  app:
    build: .
    container_name: migraciones_poc
    restart: always
    ports:
      - "8081:8081"

#version: '3.8'
#
#services:
#  jenkins:
#    image: jenkins/jenkins:lts
#    ports:
#      - "8080:8080"
#      - "50000:50000"
#    volumes:
#      - jenkins_home:/var/jenkins_home
#    restart: always
#
#volumes:
#  jenkins_home: